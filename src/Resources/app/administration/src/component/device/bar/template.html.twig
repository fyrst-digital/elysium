<div :style="viewStyle(styles.container)">
    <label :style="viewStyle(styles.label)">Geräteansicht</label>
    <blur-device-switch
        layout="row"
        :show-label="false"
        :style="{
            '--container-gap': '12px',
            '--icon-padding': '6px',
            '--icon-bg-color': '#ffffff',
            '--icon-hover-bg-color': 'var(--color-gray-100)',
        }"
        :device="device"
        @change-device="device => elysiumUI.setDevice(device)"
    />
</div>