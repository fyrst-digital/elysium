<blur-section
    gap
    padding
    :style="viewStyle({
        mobile: {
            '--blur-section-gap-y': '8px',
            '--container-gap-y': '12px'
        },
    })"
>
    <blur-column :cols="12">
        <py-select-field 
            :options="[
                { value: 'custom', label: $tc('blurElysiumSlides.forms.slideLinking.typeCustom') },
                { value: 'product', label: $tc('blurElysiumSlides.forms.slideLinking.typeProduct') },
            ]"
            :hideClearableButton="true"
            :label="$tc('blurElysiumSlides.forms.slideLinking.typeLabel')"
            v-model="slide.slideSettings.slide.linking.type"
            />
    </blur-column>

    <blur-column
        v-if="slide.slideSettings.slide?.linking?.type === 'custom'"
        :cols="12"
    >

        <py-text-field 
            v-model="slide.url"
            :label="$tc('blurElysiumSlides.forms.slideLinking.customUrl')"
            :placeholder="$tc('blurElysiumSlides.forms.slideLinking.customUrlPlaceholder')"
            :tooltip="$tc('blurElysiumSlides.forms.slideLinking.customUrlHelpText')"
            />
    </blur-column>

    <blur-column
        v-if="slide.slideSettings.slide?.linking?.type === 'product'"
        :cols="12"
    >
        <py-entity-field 
            entity="product"
            :label="$tc('blurElysiumSlides.forms.slideLinking.selectProduct')"
            :error="validateProduct"
            v-model:value="slide.productId"
            />
    </blur-column>

    <blur-column :cols="12">
        <py-text-field 
            v-model="slide.buttonLabel"
            :label="$tc('blurElysiumSlides.forms.slideButtonLabel.label')"
            :placeholder="$tc('blurElysiumSlides.forms.slideButtonLabel.placeholder')"
            :tooltip="$tc('blurElysiumSlides.forms.slideButtonLabel.helpText')"
            :disabled="slide.slideSettings.slide.linking.overlay === true"
            />
    </blur-column>

    <blur-column :cols="12">
        <py-select-field 
            :label="$tc('blurElysiumSlides.forms.slideButtonAppearance.label')"
            :tooltip="$tc('blurElysiumSlides.forms.slideButtonAppearance.helpText')"
            :disabled="slide.slideSettings.slide.linking.overlay === true"
            :hideClearableButton="true"
            :options="buttonColors"
            v-model="slide.slideSettings.slide.linking.buttonAppearance"
            />
    </blur-column>

    <blur-column :cols="12">
        <py-select-field 
            :label="$tc('blurElysiumSlides.forms.slideButtonSize.label')"
            :tooltip="$tc('blurElysiumSlides.forms.slideButtonSize.helpText')"
            :disabled="slide.slideSettings.slide.linking.overlay === true"
            :hideClearableButton="true"
            :options="buttonSizes"
            v-model="slide.slideSettings.slide.linking.buttonSize"
            />
    </blur-column>

    <blur-column 
        :cols="12"
        :style="viewStyle({
            mobile: {
                'padding-block': '12px',
                'display': 'flex',
                'flex-direction': 'column',
                'gap': '8px',
            },
        })">

        <mt-switch
            v-if="slide.slideSettings.slide?.linking?.type === 'product'"
            @change="slide.slideSettings.slide.linking.showProductFocusImage = $event"
            :checked="slide.slideSettings.slide.linking.showProductFocusImage"
            :label="$tc('blurElysiumSlides.forms.slideLinking.showProductFocusImage')"
        />

        <mt-switch
            v-if="slide.slideSettings.slide?.linking?.type === 'product'"
            @change="slide.slideSettings.slide.linking.showProductTitle = $event"
            :checked="slide.slideSettings.slide.linking.showProductTitle"
            :label="$tc('blurElysiumSlides.forms.slideLinking.showProductTitle')"
        />

        <mt-switch
            v-if="slide.slideSettings.slide?.linking?.type === 'product'"
            @change="slide.slideSettings.slide.linking.showProductDescription = $event"
            :checked="slide.slideSettings.slide.linking.showProductDescription"
            :label="$tc('blurElysiumSlides.forms.slideLinking.showProductDescription')"
        />

        <mt-switch
            @change="slide.slideSettings.slide.linking.openExternal = $event"
            :checked="slide.slideSettings.slide.linking.openExternal"
            :label="$tc('blurElysiumSlides.forms.slideLinkingOpenExternal')"
        />

        <mt-switch
            @change="slide.slideSettings.slide.linking.overlay = $event"
            :checked="slide.slideSettings.slide.linking.overlay"
            :helpText="$tc('blurElysiumSlides.forms.slideLinkingOverlay.helpText')"
            :label="$tc('blurElysiumSlides.forms.slideLinkingOverlay.label')"
        />
    </blur-column>
</blur-section>